"""Built-in example/stdlib workflows.

Workflows here are *runnable definitions*: each module exposes a
`build()` function returning a `Workflow` and a `make_engine()` factory
suitable for use with `requiem run <module>`.
"""
